@model Category

<div class="container-fluid py-4">
    <a asp-action="Index" class="text-decoration-none text-secondary small fw-medium d-inline-flex align-items-center gap-1 mb-3">
        <i class="bi bi-arrow-left"></i> Back to categories
    </a>
    <div class="card border shadow-sm overflow-hidden">
        <div class="card-header text-white py-3 px-4" style="background: linear-gradient(to right, #065f46, #282828, #191414);">
            <h5 class="mb-0 fw-bold"><i class="bi bi-plus-lg me-2"></i>Create Category</h5>
            <p class="mb-0 small" style="color: #1DB954;">Add a new category for your products</p>
        </div>
        <div class="card-body p-4">
            <div asp-validation-summary="ModelOnly" class="text-danger small"></div>
            <form method="post">
                <div class="mb-3">
                    <label asp-for="@Model.Name" class="form-label fw-semibold small"></label>
                    <input asp-for="@Model.Name" type="text" name="Name" class="form-control" placeholder="Enter category name" />
                    <span asp-validation-for="@Model.Name" class="text-danger small"></span>
                </div>
                <div class="mb-3">
                    <label asp-for="@Model.DisplayOrder" class="form-label fw-semibold small"></label>
                    <input asp-for="@Model.DisplayOrder" type="number" name="DisplayOrder" class="form-control" placeholder="Enter display order" />
                    <span asp-validation-for="@Model.DisplayOrder" class="text-danger small"></span>
                </div>
                <div class="d-flex gap-2 mt-4">
                    <button type="submit" class="btn btn-accent fw-semibold">Create Category</button>
                    <a asp-action="Index" class="btn btn-light fw-medium">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts
{
    <partial name="_ValidationScriptsPartial" />
}
